# A16：参数辨识筛查、证据记录与 revision 2 拒绝门

日期：2026-09-24  
基线：`main` / `41796e52928768c75b5ee9d162ed405bb1d19c0a`，包含 A0～A15 未提交工作区

## 1. 结论

A16 建立了可追溯的参数采集 session、原始证据哈希、离线筛查报告和安全拒绝测试，
并完成一次 5 s 受限开环实机采集。结果没有资格生成 revision 2：

- `Rs` 仅得到控制器电压指令口径的筛查值 `5.8265625 Ω`；
- `Ls` 因 50 Hz trace 不能解析电气瞬态而无结果；
- 磁链因没有实测相电压和独立转速/角度真值而无结果；
- 工具固定输出 `candidate_generation_allowed=false`，没有创建 revision 2，没有改变
  revision 1、CRC、审批位或固件。

这是“证据门正确拒绝”，不是辨识失败后绕过门限。

## 2. 实现

| 路径 | 内容 |
|---|---|
| `profiles/schema/fluxrt-identification-session-v1.schema.json` | 目标身份、工况、能力、安全限值和证据格式 |
| `tools/foc_identification_tool.py` | 哈希、CSV、运行健康、安全包络和筛查估算 |
| `tests/profile/test_foc_identification_tool.py` | 正常筛查、篡改、过流、故障位、闭环和日志缺失拒绝测试 |
| `profiles/identification/sessions/a16-screening-20260924.json` | 本次实机 session |
| `profiles/identification/evidence/a16-screening-20260924/` | 原始 CSV/串口日志 |
| `profiles/identification/reports/a16-screening-20260924.json` | 确定性筛查报告 |
| `docs/parameters/参数辨识采集与筛查流程.md` | 操作与下一测量链要求 |

## 3. 实机证据

条件：NUCLEO-G431RB + X-NUCLEO-IHM16M1 + GBM2804H-100T，A14 Diagnostic，
12/12 kHz，开环 582 rpm，启动电流 0.8 A，软件 trip 1.15 A，约 12.3 V，电源最大
2 A，空载自由旋转，5 s，50 Hz trace。

| 证据 | 结果 |
|---|---|
| 固件 BIN | 126,748 B；SHA-256 `175CD0D3FCB1F95AB2C7AAC427BC6DBD3C6AFCEE686E3D8A0B23F14FB79A5FC9` |
| CSV | SHA-256 `2E880633D36AC7311098818E9B24751FFBCC42F1E75312D30DFA55F1F3CD923F` |
| 串口日志 | SHA-256 `B4AA618B4D51959E819D3D5594D7C8E5025F54D3940433035E54687C9C4C5AF6` |
| 样本/时长 | 250 / 4.98 s |
| 峰值相电流 | trace 0.921 A；平台全速峰值约 0.941 A |
| 母线 | 12.288 V |
| dropped/error/miss/fault | 0/0/0/0 |
| 最终安全状态 | `foc_stop`；TIM1 输出关闭，duty 500/500/500 中性值 |

观察器在本次开环采集中未通过可靠性门，最后缺最低速度和方差门；运行段 78/250 个样本
发生电压限幅。因此该数据也不适合磁链或闭环质量推断。

## 4. 验证等级

- S0：schema/JSON/Python 语法与证据 SHA-256 一致；
- S1：profile/identification 共 12 项测试 PASS；完整 `test.ps1` PASS：Rust
  69+16+11+15、仿真、Clippy、两种 M4F 库、C Host 1/1；
- S5：仅上述 5 s 空载开环受限采集；没有闭环、参数阶跃或故障注入；
- 未达到 S6，没有参数审批、闭环审批或 production ready 结论。

## 5. 下一步

A17 先做 IHM16M1 相电压只读采样和短时高频辨识缓冲的架构/Host 测试；确认 PC0/PC1/
PC3 映射、分压比例、ADC 采样窗口和安全关断后，才设计受限电压阶跃。相电压数据在与
示波器/已知电压对拍前不得进入控制律。
