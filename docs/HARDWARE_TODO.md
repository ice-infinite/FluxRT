# FOC 硬件信息待办表

在实现真实外设前，需要补齐以下信息。

## MCU 与时钟

- [x] 初始测试组合：NUCLEO-G431RB + X-NUCLEO-IHM16M1（三分流）+ GBM2804H-100T。
- [x] 测试板 MCU：STM32G431RBT6，LQFP64，128 KiB Flash，32 KiB SRAM。
- [ ] 最终自研控制板的 MCU 完整料号、封装和存储容量。
- [x] NUCLEO-G431RB 使用板载 X3 24 MHz HSE，PLL 输出 170 MHz。
- [x] 低速时钟选择外部 32.768 kHz LSE，并预选为 RTC 时钟源。
- [ ] 确定 HSE/LSE 晶振具体型号、精度、ESR、负载电容和 PCB 布局。
- [ ] 在真实 PWM 上线前，将 HSE 时钟故障检测接入硬件安全关断和故障状态机。
- [x] NUCLEO 实机 SWD/ST-LINK、Flash 启动选项字节和 COM6 已验证；最终自研板策略仍待确定。

## 功率级

- [x] 参考板栅极驱动器：X-NUCLEO-IHM16M1 / STSPIN830；PB13/PB14/PB15 三相使能已主动拉低并实机回读确认。
- [x] 参考母线标称 13 V，板卡范围 7 V～45 V；实机 ADC 已读到约 12.28 V，实验电源限流和硬件保险仍待确认。
- [x] ST 参考 PWM 为 30 kHz、STSPIN830 硬件死区 550 ns；16 kHz 实机出现 9,806/9,800 和 9,810/9,800 的截止超限后，当前工程为 Rust SMO 留足 WCET 余量改为 12 kHz。
- [x] 参考映射确认：TIM1 PA8/PA9/PA10 对应 U/V/W 高侧命令。
- [x] TIM1 已配置为中心对齐约 12 kHz，ARR 约 7083，Break2=PA11；内部时基持续运行，默认及停止后 CH1～3/MOE/三相使能均关闭。
- [x] ADC1/ADC2 已读取 IU/IV/IW、母线、温度和电位器原始值并完成静态零点采样。
- [x] ADC 已改为 TIM1 TRGO 同步注入采样并接入约 12 kHz ISR；实机 5 秒约 60,000 个控制周期。
- [x] PA11/Break2 active-low 输入已配置且实机静态读高；尚未用故障注入验证硬件过流关断链。
- [x] 显式 `foc_trial` 首轮：2.3 秒、最高 80 rpm 开环命令、0.55 V 电压矢量，峰值 70 counts（约 112 mA），无保护触发并自动关断。
- [x] 显式 `foc_start` 已跑通 Rust Id/Iq 电流环和 582 rpm 强制角度启动；5 秒保持 97,569 步、0 错误、0 超时，峰值约 0.904 A。
- [x] 已现场观察两轮开环和两轮闭环均为预期正方向并持续转动；仍无编码器/测速仪，串口 rpm 是 SMO 估算值而非独立真值。

## 电流与电压采样

- [x] 参考工程为三分流采样。
- [x] 参考工程 Rshunt=0.33 Ω、放大倍数 1.53、ADC 参考 3.3 V；实物误差仍待校准。
- [x] ADC 通道已按 ST 参考映射；三相电流用 6.5-cycle TIM1 同步注入采样，其他监测量保留低频规则组读取。
- [ ] 当前已取得 U/V/W 静态零点约 1951/1933/1953 counts；临时软件过流为 220 counts（约 351 mA），温漂、阈值误差和量产校准方法待验证。
- [x] 参考母线分压比 0.0625，12-bit ADC 实测 952 counts，换算约 12.28 V；量产容差仍待校准。

## 位置与速度

- [x] 当前默认后端为可移植 Rust SMO+BEMF-PLL；STO-PLL 仅保留适配入口，闭环切换默认关闭。
- [x] 16 kHz SMO/PLL 历史调谐：保持段估速均值 582.3 rpm、标准差 22.3 rpm，126/126 个 50 Hz 样本可靠；参数已进入版本化配置，不再写死。
- [ ] 使用编码器、霍尔或外部测速仪取得独立转速/角度真值；当前观察器可能受强制电压场牵引，未完成真值交叉验证前禁止接管。
- [x] PC 同构仿真已验证：可靠后才开始 25 ms 最短路径角度/Iq 渐变、速度 PI 无阶跃接入、未收敛超时和闭环失锁停机。
- [x] 已在 2 A 限流、空载实机上完成两轮 5 秒接管：约 2.10 s 进入 transition、约 2.12 s 进入 closed-loop，状态 7 保持到结束，0 错误、0 deadline miss。上电默认继续保持 `closed_loop_enable=0`。
- [x] 参考电机极对数 7、起始电角度 0°；当前接线下开环和闭环试验方向一致。绝对电角度/相序仍需独立传感器校准。
- [ ] 编码器分辨率、定时器模式和溢出处理。

## 电机参数

- [x] 参考值：Rs=5.29 Ω、Ld=Lq=1.058 mH、5.0 V RMS/kRPM；仍需实测。
- [x] 参考值：7 极对、额定电流 0.8 A、最高 1572 rpm；峰值能力仍待实测。
- [ ] 转动惯量、负载范围和温升限制。

当前已把上述值用于 PC/MATLAB 仿真、同步采样、低压开环以及两轮短时闭环试转。短时通过不等于量产验证；独立真值、参数辨识、故障注入、冷启动统计、负载与温升验证完成前，不能用于正常或高功率运行。
